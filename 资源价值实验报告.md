# 卡片解码者：资源价值实验报告

## 1. 目的

本实验估计提示与挑战在相同题目状态下的边际价值。兑换率只用于解释、搜索排序和构造可行下界，不作为严格目标函数中的固定权重。

严格目标保持为：预期解题数最大，其次预期首次相符项数最大；两者相同后才减少无意义行动。

## 2. 数据与方法

- 数据库：大师决斗卡池的 8,019 个有效六项判定组。
- 先验：按组内大师决斗卡片数加权。
- 配对比较：同一目标、同一已知线索、同一候选后验下，分别增加一次提示或一次挑战。
- 挑战反馈包含六项相符掩码，以及边框和等级类字段相符后揭示的目标具体值。
- 提示从未知字段中均匀随机，并按字段值更新后验。

边际量定义为：

\[
\Delta_H(s)=V(s,h+1,c)-V(s,h,c)
\]

\[
\Delta_C(s)=V(s,h,c+1)-V(s,h,c)
\]

解题价值兑换率为：

\[
\rho(s)=\Delta_H^{solve}(s)/\Delta_C^{solve}(s)
\]

## 3. 实验A：小型自然后验的完整穷举

- 固定随机种子：`0x5eed1234`。
- 36个由真实卡池生成的后验状态。
- 每个状态2–7个候选组、已知2–5项。
- 比较 H0C1、H1C1、H0C2、H1C2。
- 每个状态对全卡池挑战按完整反馈签名合并，随后完整 Bellman 穷举。

完整穷举结果：

| 指标 | 结果 |
|---|---:|
| 增加一次提示的平均解题边际值 | 0.428 |
| 增加一次挑战的平均解题边际值 | 0.477 |
| 提示/挑战边际价值比中位数 | 0.944 |
| 兑换率25%–75%区间 | 0.667–1.104 |
| H1C1状态选择先提示 | 100% |
| H1C2状态选择先提示 | 13.9% |

这说明提示价值强烈依赖剩余挑战数：只有一次挑战时，提示能先区分目标；有两次挑战时，第一次挑战本身既可能解题，也能产生反馈，因此多数状态改为先挑战。它不是“始终先提示”或“始终先挑战”。

模型同时加入第三级破同分目标——在解题数和相符项相同后，减少期望行动数。这个目标只能消除无意义操作，不能覆盖前两个收益目标。

## 4. 实验B：接近开局的大候选状态

- 固定随机种子：`0x91ab22ef`。
- 10个真实目标和随机初始字段形成的配对状态。
- 平均候选组数：1,472.4。
- 每个状态使用相同的500张挑战行动库；比较策略共享完全相同的行动库。
- 一次提示后允许根据实际提示结果自适应选择挑战。
- 两次挑战策略先比较16个首轮分支，再根据挑战反馈自适应选择第二次挑战。

这一实验是“受限自适应策略树”的配对估计，不是全动作空间最优证明。

主要结果：

| 指标 | 结果 |
|---|---:|
| 一次挑战平均直接通关率 | 1.25% |
| 增加一次提示后的平均通关增量 | 5.66个百分点 |
| 增加一次挑战后的平均通关增量 | 9.30个百分点 |
| 提示/挑战解题边际价值平均比 | 0.643 |
| 兑换率中位数 | 0.608 |
| 25%–75%区间 | 0.524–0.765 |

解释：在这批接近开局的受限策略树中，一次提示对解题概率的边际贡献约为一次挑战的0.61倍（中位数）；在线索充分的小状态中，中位数上升到0.94。场景差异足以否定全局固定兑换率。

## 5. 结论与限制

1. 提示具有显著且可测的决策价值；大状态固定返回挑战没有数学依据。
2. 提示与挑战不存在稳定的固定兑换比例。当前0.61仅是接近开局样本中的经验中位数。
3. 小候选、线索充分且只有一次挑战时，提示通常接近一次挑战的价值；挑战增至两次后，先挑战往往更好。
4. 实验B限制了挑战行动库和首轮分支，结果只能用于搜索排序和下界策略。
5. 正式求解器必须让提示与挑战进入同一棵自适应策略树，并报告搜索深度、上下界及是否已证明。
6. 后续应扩大样本并按候选有效规模、未知字段数、剩余库存和剩余题数分层；在这些数据完成前，不报告单一“官方兑换率”。

## 6. 对求解器的直接约束

- 严格价值向量采用 `(预期解题数, 预期首次相符项数, -预期行动数)`。
- 第三维只在前两维相同时破同分。
- 提示和挑战必须接受同样的搜索深度与终止规则。
- 大状态至少比较“先提示”的完整随机反馈分支与多个挑战反馈分支。
- 经验兑换率只决定优先展开顺序，不得改变 Bellman 比较结果。
- 搜索边界重叠时显示“未证明”，不得把当前下界动作包装成全局最优。

原始结果保存在开发目录的 `resource-value-results.json` 和 `resource-value-opening-results.json`，随机种子固定，可重复生成。
