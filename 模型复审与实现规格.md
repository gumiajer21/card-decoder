# 卡片解码者：模型复审与实现规格

## 一、固定目标

所有题使用相同标准。状态价值采用词典序向量：

\[
V(s)=\left(E[S],E[M],-E[A]\right)
\]

- `S`：最终解出的题数；
- `M`：由挑战首次猜中的累计项目数；
- `A`：执行的提示与挑战总次数。

第三维只在前两维相同的情况下消除无意义操作。例如单候选时，提示后挑战和直接挑战都必定解题并获得六项相符，直接挑战因行动更少而胜出。

提示和挑战不使用固定兑换权重。实验得到的约0.61只用于大状态搜索顺序和解释。

## 二、概率状态

当前题状态包含：题目进度、提示库存、挑战库存、候选组后验、已公开字段、由挑战累计相符字段、已挑战行动。目标按组内实际卡片数加权；观察后通过确定性似然做贝叶斯归一化。

初始揭示和提示只改变信息状态，不产生相符收益。挑战反馈同时编码相符掩码、相符后的目标具体边框，以及相符后的目标等级／阶级／连接值。

## 三、统一递归

提示与每张可挑战卡均属于同一个动作集合：

\[
V(s)=\max_{a}\sum_oP(o\mid s,a)\left[r(s,a,o)+V(T(s,a,o))\right]
\]

提示即时收益为零但行动数为一；挑战获得首次相符项，完全匹配时增加一道解题数，行动数同样为一。所有期望、加法和最大值均在词典序向量上进行。

求解结果是一棵依赖实际反馈的策略树，不是一条固定的“提示、挑战、挑战”序列。

## 四、计算层级

### 完整 Bellman

候选与资源规模足够小时，展开全部提示字段、字段值、挑战行动和挑战反馈。只有此模式可以显示“已证明最优”。

### 受限深度策略树

大型状态中，从全卡池即时评估结果、反馈区分能力和高后验候选中构造挑战行动集。提示与这些挑战使用相同递归，优先尝试深度3；超过状态预算时退到深度2。

该结果是合法可执行策略的下界，不是全卡池全活动证明。界面必须显示搜索深度和“当前可行下界”。

受限深度无法直接展开全部后续题。为避免把挑战和提示都当作无未来价值的一次行动，当前在“完全猜中”叶节点加入跨题资源续值：

\[
\hat W(r,h,c)=\min\left(r,c,\frac{c+0.61h}{3.9}\right)
\]

其中 `r` 为后续题数；0.61来自开局大候选资源实验的提示/挑战边际价值中位数；3.9来自50轮、9题、11提示、36挑战实测所反推的每题等价资源消耗。该续值只用于搜索截断处，当前题内部仍按完整随机反馈递归。它是数据校准的近似价值函数，不是最优性证明，也不是“挑战不足时强制提示”的条件规则。

### 分支定界

每个动作维护可行下界和安全上界。只有某动作下界不低于其他动作上界时，才标记已证明。边界重叠时不得把当前下界动作表述为全局最优。

## 五、资源价值实验的作用

经验边际价值用于：

1. 决定优先展开提示还是挑战分支；
2. 选择受限挑战行动集；
3. 构造搜索截断后的合法策略；
4. 向用户解释提示在同类状态中的通常作用。

经验值不得进入严格价值向量，不得作为剪枝上界，也不得用于声称最优。

## 六、验证要求

- 单候选直接挑战；
- 两候选、一次挑战且提示可区分时先提示；
- 两候选、两次挑战足以保证解题时先挑战；
- 增加资源不能降低前两维最优价值；
- 提示和挑战采用相同的递归深度与终止规则；
- 独立参考枚举器与生产 Bellman 在随机小卡池上比较动作集合和完整价值向量；
- 大状态界面准确区分“精确证明”和“策略树下界”。

## 七、当前边界

当前大型状态实现的是受限行动集、深度2–3的单题自适应策略树。它已经恢复提示/挑战的真实比较，但尚未完成跨全部后续题的深层分支定界。因此它会报告可行下界，不会把结果称为全局最优。下一步性能工作应集中在动作等价合并、后验状态缓存和更紧的后续题上界，而不是重新引入人工提示阈值。
